# Cyntr security audit — 2026-06-05

Full-repo multi-agent audit (22 review units, 165 agents): 137 verified findings (112 CONFIRMED, 25 PLAUSIBLE).

## CRITICAL (13)

| category | file:line | summary |
|---|---|---|
| tenant-isolation | `modules/agent/runtime.go:947` | handleSessionMessages reads any session's full history by raw session ID with no tenant/agent authorization |
| tenant-isolation | `modules/agent/store.go:292` | SearchMessages (wired to agent.search) runs full-text search across ALL tenants' messages with no tenant filter |
| tenant-isolation | `modules/agent/tools/delegate.go:34` | DelegateTool takes the target tenant from model-supplied input instead of the caller's tool context, allowing cross-tenant agent invocation. |
| tenant-isolation | `modules/audit/query.go:14` | Audit query has no enforced tenant scoping: caller-supplied Tenant filter is optional, so any authenticated read-scoped caller can read every tenant's |
| authn | `auth/oidc.go:240` | ID token signature verification silently bypassed when JWKS fetch fails ('graceful degradation') |
| authn | `auth/oidc.go:379` | ExchangeCode (non-PKCE path) issues a session without ANY ID token signature verification |
| authn | `modules/channel/discord/adapter.go:86` | Discord interactions endpoint skips Ed25519 signature verification, allowing spoofed slash-command interactions. |
| authn | `modules/channel/slack/adapter.go:166` | Slack Events webhook does not verify the X-Slack-Signature / signing secret, so any unauthenticated caller can inject inbound messages. |
| authn | `modules/channel/teams/adapter.go:105` | Teams activity endpoint accepts unauthenticated activities (no Bot Framework JWT validation) and trusts a request-supplied serviceUrl for replies (SSR |
| tenant-isolation | `modules/channel/webhook/adapter.go:110` | Webhook ingress trusts attacker-supplied tenant and agent from the request body with no authentication or signature verification, allowing cross-tenan |
| injection | `modules/mcp/manager.go:107` | mcp.server.add bus handler spawns an arbitrary subprocess from caller-supplied Command/Args/Env with no authz check, yielding remote code execution. |
| tenant-isolation | `modules/federation/module.go:165` | Inbound delegation never verifies the peer shared secret and never checks the caller is authorized for the requested tenant/agent, allowing cross-tena |
| authn | `web/api/middleware.go:111` | JWT path accepts ANY token without verifying the signature, granting full access when JWTSecret is configured. |

## HIGH (51)

| category | file:line | summary |
|---|---|---|
| authz | `modules/agent/runtime.go:1276` | handleMemoryDelete deletes any long-term memory by ID with no tenant/agent ownership check |
| concurrency | `modules/agent/runtime.go:422` | One shared Session per (tenant,agent) is mutated across concurrent chats, interleaving different users' history and lastUser |
| tenant-isolation | `modules/agent/runtime.go:1398` | Auto-memory is stored and recalled per (agent,tenant) only, leaking one user's facts into another user's prompt |
| injection | `modules/agent/tools/aws.go:50` | account_id and role_name are interpolated unescaped into a bash -c assume-role command, enabling shell command injection. |
| injection | `modules/agent/tools/aws_cost.go:61` | group_by is interpolated unescaped into a bash -c AWS CLI command without allowlist validation, enabling command injection. |
| path-traversal | `modules/agent/tools/file.go:21` | FileRead/FileWrite/FileSearch operate on arbitrary absolute or ../ paths with no base-directory confinement or tenant scoping. |
| ssrf | `modules/agent/tools/http.go:37` | http_request fetches any caller-supplied URL with no host/scheme/IP restrictions, enabling SSRF to cloud metadata and internal services. |
| concurrency | `modules/agent/tools/knowledge.go:76` | Unsynchronized read of t.embedder.vocabulary in Execute races with map writes by the rebuildEmbeddings goroutine, risking a fatal concurrent-map-read/ |
| tenant-isolation | `modules/agent/tools/knowledge.go:29` | KnowledgeTool stores and searches all documents in a single global FTS table with no tenant column, so every tenant's agent can read every other tenan |
| correctness | `modules/audit/emitter.go:27` | Audit entry IDs are generated only to millisecond precision, causing primary-key collisions that silently drop audit records under load. |
| authz | `modules/audit/logger.go:74` | audit.query IPC handler performs a privileged read with no authorization check on the caller's identity, role, or tenant. |
| ssrf | `auth/oidc.go:281` | fetchJWKS uses default http.Get with no context and no timeout against an issuer-controlled URL |
| authz | `auth/session.go:119` | CreateAPIKey silently grants full admin scope when no scopes are specified |
| concurrency | `modules/channel/slack/adapter.go:390` | a.routes map is written in handleSlashCommands and lazily initialized/read in handleEvents goroutines without any synchronization, causing a data race |
| authn | `modules/channel/telegram/adapter.go:86` | Telegram webhook has no secret-token validation, so anyone can post fake updates to drive the agent and send messages to arbitrary chats. |
| authn | `modules/channel/email/adapter.go:87` | Email inbound endpoint is unauthenticated and trusts the JSON 'from' field as the user identity, enabling trivial sender spoofing and gate bypass. |
| authn | `modules/channel/googlechat/adapter.go:95` | Google Chat events endpoint does not verify the Google-signed bearer JWT, allowing spoofed events and message injection. |
| ssrf | `modules/channel/matrix/adapter.go:91` | Untrusted inbound room_id is interpolated unescaped into the homeserver request URL path, enabling path injection / SSRF against arbitrary Matrix Clie |
| authn | `modules/channel/mattermost/adapter.go:104` | Mattermost slash-command handler does not verify the Mattermost-provided command token, so anyone can invoke the agent and impersonate arbitrary users |
| authn | `modules/channel/sms/adapter.go:111` | Twilio inbound webhook (HandleInbound) does not validate the X-Twilio-Signature, allowing forged SMS messages and sender spoofing. |
| authn | `modules/channel/whatsapp/adapter.go:94` | WhatsApp inbound webhook does not verify the X-Hub-Signature-256 HMAC, so any unauthenticated client can inject spoofed inbound messages. |
| concurrency | `kernel/ipc/bus.go:121` | Send on closed channel panic: Request releases the lock before sending to entry.inbox, and Close() closes that inbox under the write lock — a concurre |
| concurrency | `modules/eval/runner.go:60` | r.counter++ is incremented without holding r.mu in handleRun, but bus handlers run concurrently (one goroutine per request per kernel/ipc/bus.go:69-82 |
| concurrency | `modules/eval/runner.go:132` | executeRun mutates the shared *EvalRun (run.Results, run.Status, run.TotalScore, etc.) with no lock, while handleStatus returns the same pointer and h |
| concurrency | `modules/crew/engine.go:102` | Unsynchronized counter increment and unlocked mutation of CrewRun fields read concurrently by status handlers. |
| tenant-isolation | `modules/curator/store.go:200` | Invocation queries are keyed only by skill_name, never tenant, so judge/scores/prune leak one tenant's data to another. |
| ssrf | `modules/workflow/engine.go:422` | Webhook step fetches a fully caller-controlled URL with no scheme/host allowlist (SSRF). |
| authz | `modules/workflow/engine.go:637` | Approval step fails open: any error from the policy module auto-approves the step. |
| concurrency | `modules/workflow/engine.go:499` | Concurrent unsynchronized writes to run.Results map in parallel/loop steps while other handlers read it under RLock. |
| concurrency | `modules/policy/approval.go:138` | ListPending mutates request state (r.Status = ApprovalExpired) while holding only an RLock, a data race against concurrent Approve/Deny/Submit holding |
| authz | `modules/policy/engine.go:94` | Approval approve/deny/get/list bus handlers perform privileged decisions with no caller identity, role, or tenant check; decided_by is attacker-suppli |
| tenant-isolation | `modules/proxy/handler.go:46` | Tenant and user identity used for the policy decision are taken from unauthenticated client-supplied headers (X-Cyntr-Tenant / X-Cyntr-User), allowing |
| dos | `modules/proxy/parsers/anthropic.go:30` | Unbounded io.ReadAll of request body in intent parser before any policy/size check buffers the entire attacker-supplied body into memory. |
| dos | `modules/proxy/ratelimit.go:37` | RateLimiter.buckets map grows without bound and is keyed by the attacker-controlled X-Cyntr-Tenant header; entries are never evicted. |
| authz | `modules/skill/executor.go:102` | SandboxedExecutor performs no actual capability enforcement; the 'sandbox' is a no-op that always delegates to the inner shell executor. |
| ssrf | `modules/skill/marketplace.go:94` | Marketplace.Download fetches an arbitrary, unvalidated DownloadURL server-side (SSRF), and writes the response to disk. |
| authz | `modules/skill/runtime.go:188` | Auto-activated 'safe' candidate can silently overwrite an existing trusted skill (e.g. an embedded catalog skill) of the same name via ReplaceWithBack |
| path-traversal | `modules/skill/runtime.go:147` | skill.import_openclaw and skill.install accept an arbitrary filesystem path from the bus payload and read/execute it with no base-directory confinemen |
| authz | `modules/federation/module.go:188` | Policy enforcement in inbound delegation fails open on any policy error other than ErrNoHandler, and treats RequireApproval as Allow. |
| authn | `modules/federation/module.go:212` | Caller-supplied identity fields (req.Caller, req.User) are trusted and embedded into the downstream agent user identity without authentication. |
| ssrf | `modules/federation/peer.go:80` | Peer.Endpoint is an attacker-supplied URL fetched server-side by health checks, queries, and sync with no host allowlist, enabling SSRF. |
| authz | `web/api/curator.go:116` | Curator admin operations (judge/prune/consolidate) are 'gated' by a client-supplied request header X-Cyntr-Admin: 1, which any caller can set, and oth |
| path-traversal | `web/api/knowledge.go:51` | handleKnowledgeIngest reads an arbitrary server-side file path supplied in the request body (file_path) via os.ReadFile with no allow-list or sandbox, |
| authn | `web/api/middleware.go:111` | JWT branch accepts ANY bearer token without verifying the signature, expiry, or claims when JWTSecret is configured — a complete authentication bypass |
| ssrf | `web/api/skills.go:210` | handleSkillMarketplaceInstall fetches an arbitrary, fully user-controlled download_url server-side (marketplace.Download -> http.Get on the raw URL) w |
| path-traversal | `web/api/toolbuilder.go:98` | handleToolCreate and handleToolDelete build a filesystem path from the unvalidated tool name (body.Name / path value), allowing writing or deleting ar |
| authz | `web/api/users.go:126` | handleAuthMe returns {"user":"admin","role":"admin"} for ANY token whose hash is not found, so unknown/garbage credentials are reported as an admin id |
| authz | `web/api/curator.go:116` | requireCuratorAdmin can be satisfied by any caller simply sending the header X-Cyntr-Admin: 1, defeating the admin gate on cost-incurring/destructive  |
| path-traversal | `web/api/knowledge.go:51` | handleKnowledgeIngest reads an arbitrary server-side file path supplied by the client and returns its contents via the knowledge base. |
| path-traversal | `web/api/toolbuilder.go:98` | handleToolCreate/handleToolDelete build a file path from unsanitized user-controlled `name`, allowing arbitrary file write and delete outside tools/. |
| authz | `web/api/webhooks.go:14` | Webhook handlers run privileged workflow/agent actions using fully client-controlled tenant/agent/workflow IDs with no per-webhook secret or tenant au |

## MEDIUM (42)

| category | file:line | summary |
|---|---|---|
| correctness | `modules/agent/providers/anthropic.go:180` | For base64 inline images the full data URI (including the `data:<mime>;base64,` prefix) is placed in the Anthropic `source.data` field, which expects  |
| secret-exposure | `modules/agent/providers/gemini.go:65` | Gemini API key is placed in the request URL query string (?key=<apiKey>) instead of a header, so the secret leaks into any logging/observability that  |
| secret-exposure | `modules/agent/runtime.go:808` | Per-agent secrets injected into tool input are surfaced back to the model verbatim when a tool returns an error echoing its arguments |
| ssrf | `modules/agent/tools/database.go:36` | database_query accepts an arbitrary caller-supplied DSN and connects to it, enabling SSRF/internal port-scanning and connections to internal databases |
| correctness | `modules/audit/emitter.go:52` | EmitAgentChat encodes message length via string(rune(len(message))), producing a single (often invalid/garbage) Unicode code point instead of the nume |
| dos | `modules/audit/query.go:44` | Audit query enforces no upper bound on Limit (and treats Limit<=0 as unlimited), allowing unbounded result sets. |
| authz | `auth/module.go:27` | auth.resolve bus handler performs credential->principal resolution with no caller authentication/authorization |
| authn | `auth/oidc.go:194` | Audience claim typed as string and skipped when empty; multi-valued/absent aud not validated |
| dos | `modules/channel/discord/adapter.go:92` | Webhook handlers read the entire request body with io.ReadAll and no size limit across all four adapters, allowing memory-exhaustion DoS. |
| dos | `modules/channel/email/adapter.go:93` | Inbound handlers read the entire request body with io.ReadAll and no size limit, allowing memory-exhaustion DoS from a single large unauthenticated PO |
| dos | `modules/channel/whatsapp/adapter.go:141` | Inbound WhatsApp webhook spawns an unbounded goroutine per message with no concurrency limit, no rate limit, and no per-call timeout, enabling gorouti |
| tenant-isolation | `modules/channel/manager.go:253` | handleProgress and handleSend dispatch outbound messages purely on attacker-/event-supplied channel + channel_id with no tenant scoping, so a crafted  |
| dos | `modules/channel/webhook/adapter.go:111` | Webhook handler decodes the request body with no size limit, allowing memory-exhaustion DoS via an arbitrarily large JSON payload. |
| resource-leak | `modules/channel/webhook/adapter.go:83` | Outbound Send uses http.Post with the default client (no timeout) and ignores the caller's context, so a hung/slow outbound endpoint blocks the gorout |
| concurrency | `kernel/log/logger.go:65` | Data race on Logger.level: SetLevel writes the field with no lock while log() reads it outside the mutex. |
| dos | `kernel/ipc/bus.go:72` | Request-handler goroutine has no panic recovery; a handler panic on attacker-shaped input crashes the entire process (unlike Publish, which recovers). |
| authz | `kernel/resource/manager.go:53` | A configured limit of 0 means "unlimited" rather than "deny", so setting a resource limit to 0 to block a tenant silently grants unlimited access. |
| tenant-isolation | `modules/mcp/manager.go:95` | MCP clients are keyed only by cfg.Name with no tenant scoping, so any tenant can enumerate, query, or remove another tenant's MCP server and its tools |
| resource-leak | `modules/mcp/manager.go:85` | connectServer overwrites an existing client for the same name without closing it, leaking the previous subprocess and its goroutines. |
| ssrf | `modules/mcp/manager.go:128` | handleAddServer accepts an HTTP transport with a caller-controlled URL that the server then POSTs to, enabling SSRF to internal endpoints. |
| correctness | `modules/mcp/transport.go:57` | StdioTransport.Send does not correlate JSON-RPC response IDs to requests and treats the next stdout line as the reply, so server notifications/log lin |
| secret-exposure | `modules/usermodel/distiller.go:242` | Distiller comment claims it scrubs secrets from activity summaries, but buildSummaryBlock performs no redaction; unsanitized activity is baked into th |
| dos | `modules/usermodel/distiller.go:345` | Distiller.Tick launches one goroutine per active user with no bound; only the LLM call is throttled by the semaphore, so goroutine/memory fan-out is u |
| resource-leak | `modules/sla/module.go:163` | In Check(), m.latencies[key] is only pruned to the window when rule.MaxResponseMs > 0. For an error-rate-only rule (MaxResponseMs==0, MaxErrorRate>0), |
| injection | `modules/curator/improve.go:75` | Untrusted failure samples and current SKILL.md are interpolated into the improver prompt, enabling prompt injection that shapes an auto-proposed skill |
| correctness | `cmd/cyntr/chat.go:164` | urlEncode is a hand-rolled, incomplete percent-encoder that does not escape '%' (and turns spaces into '+'), so it both corrupts inputs containing '%' |
| injection | `packs/loomfeed/alatirok.go:692` | Agent-controlled community_slug, memory_key, sort and limit values are concatenated directly into the request URL path/query without url.PathEscape/Qu |
| dos | `packs/loomfeed/news.go:309` | Unsynchronized cache refresh causes a thundering-herd: every concurrent caller that observes a stale cache independently triggers refresh(), each spaw |
| dos | `modules/policy/approval.go:80` | ApprovalQueue.requests map grows unbounded: entries are never deleted on approve/deny/expire, and there is no sweeper. |
| correctness | `modules/quota/enforcer.go:165` | Token quota fails open on storage error: CheckTokens treats a store read error as current=0, so a tenant over budget is allowed through whenever the D |
| resource-leak | `modules/proxy/handler.go:99` | A new httputil.SingleHostReverseProxy is constructed per request and the HTTP server has no read/write/idle timeouts, enabling slowloris-style resourc |
| authz | `modules/skill/catalog.go:41` | Every embedded catalog skill is unconditionally granted full capabilities (Shell + Network '*' + Filesystem '*'), so a parsing/packaging mistake or fu |
| injection | `modules/skill/marketplace.go:44` | Search interpolates the raw query into the request URL without URL-escaping, allowing query/parameter injection and request smuggling into the marketp |
| tenant-isolation | `modules/skill/runtime.go:213` | Candidate approve/reject/rollback/list handlers ignore the caller's tenant, so any tenant can read, approve, reject, or roll back another tenant's ski |
| secret-exposure | `modules/federation/delegate.go:74` | Remote peer response bodies and endpoints are interpolated verbatim into errors that propagate back to API callers, leaking internal details. |
| tenant-isolation | `modules/federation/query.go:30` | Federated audit query fans the request to every peer and returns merged audit indexes without scoping by the caller's tenant, enabling cross-tenant au |
| tenant-isolation | `modules/federation/residency.go:34` | ResidencyPolicy is defined but never wired into the delegation/query paths, so data-residency restrictions are not enforced for federated operations. |
| dos | `modules/federation/sync.go:72` | PolicySync.sendToPeer uses the package-global http.Post with no timeout or context, so a slow/malicious peer can hang the broadcast indefinitely. |
| tenant-isolation | `web/api/middleware.go:90` | API key auth never binds the caller to a tenant; every tenant-scoped handler reads {tid} from the URL path with no check that the key belongs to that  |
| dos | `web/api/webhooks.go:18` | handleWebhookTrigger reads the entire request body with io.ReadAll and no size limit, allowing unbounded memory consumption; the endpoint also trigger |
| tenant-isolation | `web/api/agents.go:15` | Tenant-scoped agent/session/memory/profile handlers trust the {tid} path param with no check that the authenticated key belongs to that tenant; API ke |
| secret-exposure | `web/api/middleware.go:79` | API key accepted via ?key= query parameter, which leaks the secret into access logs, proxy logs, browser history, and Referer headers. |

## LOW (31)

| category | file:line | summary |
|---|---|---|
| correctness | `modules/agent/providers/anthropic.go:324` | SSE parser uses bufio.Scanner with the default 64KB token limit and silently ignores scanner errors, so a single large `data:` line (e.g. a big tool-i |
| error-handling | `modules/agent/providers/oauth.go:169` | The OAuth token-endpoint response body is decoded with the error ignored and read with no size limit, so a malformed or oversized response can yield a |
| concurrency | `modules/agent/masking.go:23` | Global customPatterns slice is mutated by LoadSecretPatterns without a lock while MaskSecrets reads it from chat goroutines |
| authn | `auth/oidc.go:117` | crypto/rand error ignored when generating PKCE code verifier |
| dos | `modules/channel/slack/adapter.go:266` | The seen sync.Map used for event deduplication grows without bound and is never evicted, leaking memory. |
| correctness | `modules/channel/slack/chunker.go:40` | chunkMessage splits on byte indices and adds an [n/m] prefix after splitting, breaking multibyte UTF-8 characters and pushing chunks past the intended |
| authz | `modules/channel/manager.go:184` | handlePairingPending returns pending pairing requests (including live pairing codes) for any tenant named in the request payload, with no check that t |
| authn | `modules/channel/pairing.go:223` | pairingCode() ignores the crypto/rand.Read error and uses modulo reduction over a 31-char alphabet, producing predictable/biased pairing codes that we |
| secret-exposure | `kernel/config/migrate.go:96` | MigrateFile writes the config back with world-readable 0644 permissions, widening access to a file that holds auth/federation identifiers and tenant p |
| resource-leak | `kernel/ipc/bus.go:67` | Re-registering a handler for the same module/topic overwrites the map entry without closing the previous inbox, permanently leaking the old reader gor |
| concurrency | `kernel/ipc/bus.go:134` | Subscribe does not check the closed flag, so subscriptions can be registered against a closed bus and silently never fire. |
| error-handling | `kernel/ipc/bus.go:209` | generateID ignores the error from crypto/rand.Read; on RNG failure it emits a predictable/empty ID used for message and subscription identity. |
| correctness | `kernel/jobs/queue.go:476` | genID() ignores the error from crypto/rand.Read and can return an all-zero ID, causing PRIMARY KEY collisions and silent job loss. |
| concurrency | `kernel/jobs/queue.go:299` | leaseLocked holds q.mu across a DB query and up to leaseBatch (100) UPDATE statements; combined with run() taking the same lock to record results, a s |
| resource-leak | `kernel/jobs/queue.go:245` | Stop() can hang forever: handlers run with context.Background() and the default jobTimeout is 0, so wg.Wait() blocks indefinitely if any in-flight han |
| dos | `kernel/resource/manager.go:23` | Manager.usage and Manager.limits maps grow unbounded per tenant (and per ResourceType) and are never pruned; there is no RemoveTenant, so attacker-inf |
| error-handling | `modules/mcp/client.go:132` | CallTool ignores the json.Unmarshal error when decoding the tool result, silently returning an empty/zero ToolCallResult on malformed server output. |
| authz | `modules/mcp/server.go:45` | MCP Server (ServeHTTP and ServeStdio) exposes tools/list and tools/call for the entire global ToolRegistry with no authentication or tenant scoping. |
| correctness | `modules/recall/store.go:80` | Store.Index writes the source row and the FTS index in two separate non-transactional Execs, so a failure or crash between them desyncs the external-c |
| error-handling | `modules/eval/runner.go:169` | regexp.MatchString error is discarded in scoreOutput's regex branch; an invalid user-supplied ExpectedOutput regex silently scores 0 instead of surfac |
| tenant-isolation | `modules/sla/module.go:257` | handleListViolations (and handleListRules) return every tenant's violations/rules to any caller with no tenant filtering or caller-identity check. |
| concurrency | `modules/scheduler/scheduler.go:121` | executeJob goroutine reads *Job fields concurrently with checkAndRun mutating LastRun/NextRun on the same pointer. |
| dos | `modules/workflow/engine.go:89` | User-supplied trigger/condition regexes are compiled and run on every event with no validation (ReDoS + recompile cost). |
| secret-exposure | `cmd/cyntr/init.go:545` | The init wizard prints a freshly generated CYNTR_API_KEY (truncated) to the terminal and writes provider API keys to a .env file; while .env is 0600,  |
| resource-leak | `packs/loomfeed/alatirok.go:79` | Every authenticated action fires a detached heartbeat goroutine bound to context.Background(), ignoring the caller's context and providing no cancella |
| error-handling | `packs/loomfeed/alatirok.go:580` | createComment's dedup and auto-threading silently no-op because it json.Unmarshals the comments response as a bare []struct, but the API (per feed/com |
| authz | `modules/policy/approval.go:75` | Approval IDs are generated as predictable, sequential timestamps (apr_<UnixNano>), making them guessable for an unauthenticated approve/deny once the  |
| correctness | `modules/policy/spending.go:42` | RecordCost increments agent usage before validating the tenant has actually been charged consistently, and on agent-budget rejection the tenant counte |
| tenant-isolation | `modules/proxy/gateway.go:113` | handleList returns all registered ExternalAgents across every tenant to any caller, and handleRegister stores an agent under a caller-supplied Tenant  |
| error-handling | `modules/skill/marketplace.go:64` | JSON decode errors are silently ignored in Search and Get, so a malformed/oversized registry response is treated as success with empty/zero data. |
| nil-deref | `web/api/agents.go:324` | Unchecked type assertions resp.Payload.(string) panic the handler goroutine on an unexpected bus payload shape. |

