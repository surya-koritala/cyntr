# Twitter/X launch thread — 6 tweets

> Post Tuesday, 9:05 AM Pacific, ~5 min after the Show HN goes live.
> Pin the thread on the @cyntr account (or Surya's account). Reply
> chain, no separate tweets.

---

## Tweet 1 — hook (269 chars)

```
Today we're open-sourcing Cyntr — an AI agent platform you self-host
as a single Go binary.

OPA Rego policy-as-code. Federation across independent nodes.
Multi-tenant. Audit hash chains. Apache 2.0.

What we wished existed when we started shipping agents at work. 🧵
```

**Image:** the federation topology diagram from the README — two
nodes, one `/federation/delegate` arrow, the receiver labelled
`policy.check → allow`. Dark mode, no logo watermark.

---

## Tweet 2 — what's different (271 chars)

```
The hard part of shipping an agent isn't the prompts. It's:

– multi-tenant isolation
– a policy security can actually review
– a tamper-evident audit log
– cross-org / cross-region delegation
– catching model regressions before users do

We built for all five.
```

**Image:** a side-by-side of `policy.yaml` (left) and `policy.rego`
(right) — both with syntax highlighting, both labelled "same
evaluator." Same screenshot as gallery shot #4.

---

## Tweet 3 — federation in 2 minutes (262 chars)

```
The most concrete proof of differentiation is the federation demo.
Two independent Cyntr nodes, one cross-node delegation, the receiver's
policy gets to deny.

  go test ./demos/federation/ -v

Runs in-process in under two minutes. No Docker. No infra.
```

**Image:** terminal screenshot of the demo output — the `decision:
allow` line and the denied-by-policy error visible together. Same
screenshot as gallery shot #2.

---

## Tweet 4 — eval-as-CI (245 chars)

```
We also got tired of model upgrades silently regressing agents.

`cyntr eval ./evals/ --threshold 90` runs your test cases on every
PR and exits non-zero when pass rate drops. JUnit output, GitHub
Actions–native.

Treat agent quality like code quality.
```

**Image:** GitHub Actions check run — green ✓ for previous commits,
red ✗ on the latest, with the eval CLI output expanded underneath.

---

## Tweet 5 — fair comparison (270 chars)

```
Honest about the landscape:

Hermes — consumer leader, brilliant chat UX. Different shape.
Dify — best visual workflow builder in OSS. Different buyer.
LangChain — a library, not a platform. Complementary.

Cyntr is for the team that wants policy + audit shipped on day one.
```

**Image:** a small fragment of the comparison table from
`launch/comparison-page.md` — just the policy, federation,
multi-tenant, audit, and license rows. Dark mode.

---

## Tweet 6 — the ask (273 chars)

```
This is v1.1, not v3.0. We're early. We'd rather hear about the
rough edges now while we can still fix them.

– Repo: github.com/surya-koritala/cyntr
– Federation demo: 2 min, no infra
– Hosted sandbox: try.cyntr.dev
– Discord: in README

Star if you want to see where this goes.
```

**Image:** a clean GitHub header card — repo name, star count,
language, Apache 2.0 badge. Generated by the GitHub social preview
endpoint, not Photoshopped.

---

## Posting checklist

- [ ] Schedule for 9:05 AM PT, **after** the Show HN is live (so the
      thread can link back to the HN discussion if it picks up).
- [ ] First reply to your own thread: link to the HN post.
- [ ] Quote-RT yourself at 5 PM PT if the HN post is on the front
      page — say "thread is still going on HN if you want to argue
      about Rego."
- [ ] Avoid the temptation to add a 7th tweet. Threads die past 6.
- [ ] Do not auto-DM new followers anything. Ever.
